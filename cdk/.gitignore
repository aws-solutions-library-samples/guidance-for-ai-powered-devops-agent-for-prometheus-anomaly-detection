node_modules/
cdk.out/
dist/
*.js
*.d.ts
!jest.config.js

# Vendored Python deps are re-installed at build (deploy/10-deploy-cdk.sh runs pip install -r requirements.txt -t).
# Keep ONLY the app handler code, not the bundled libraries.
lambda/*
!lambda/jwt-authorizer.py
!lambda/requirements.txt
lambda-mcp-wrapper/lambda/*
!lambda-mcp-wrapper/lambda/awslabs/
!lambda-mcp-wrapper/lambda/*.py
!lambda-mcp-wrapper/lambda/requirements.txt

# Generated during `cdk synth` (api-gateway-lambda-stack.ts) with unresolved CDK tokens,
# and not consumed by the deploy flow (deploy/20 reads live CloudFormation outputs). Don't track.
mcp-server-config.json# Vendored deps (installed at deploy time by deploy/10-deploy-cdk.sh)
lambda-mcp-wrapper/lambda/six.py
lambda-mcp-wrapper/lambda/typing_extensions.py
